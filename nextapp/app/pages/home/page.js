"use client";

import React from "react";

const Home = () => {
  const btnsubmit = () => {
    //hmm
  };
  return (
    <div>
      <button type="button" class="button" onClick={btnsubmit}>
        balls
      </button>
    </div>
  );
};

export default Home;
